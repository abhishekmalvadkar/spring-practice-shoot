package in.spring.shoot.AOPAsyncAndSyncBehaviour;

public class Notes {
	
	/*
	 * - I had a dought that AOP advices are executing in async way or in sync way?
	 * - And after this section of practice i have understood that all the advices
	 *   of spring AOP will execute in sync way in a same thread , it will not execute in different thread.
	 */

}
