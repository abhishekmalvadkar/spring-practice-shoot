package in.spring.shoot.JAXBContext;

public class Notes {
	
	/*
	 * If you return response as plain json or xml string directly then 
	 * your caller will not able to use feature of content negosiation by setting
	 * Accept in request header.
	 */
	
}
