package in.spring.shoot.ObjectMapper;

public class Notes {
	
	/*
	 * We can use object mapper to convert java object to json string and send to different destinations like
	 * - To Redis as json string
	 * - To Kafka as json string
	 * We can use object mapper to convert json string to java object
	 * - When data recived from redis into our app then convert json string to java object
	 * - When data recived from kafka/any message queue into our app then convert json string to java object
	 */

}
